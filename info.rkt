#lang info

(define version "0.0.1")

(define deps '("hostname"
               "simple-qr"
               "urlang"
               "define-assets-from"
               ))

(define scribblings '(("scribblings/manual.scrbl" (multi-page))))
